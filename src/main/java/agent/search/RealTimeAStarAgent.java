package agent.search;

import simulator.SimulatorContext;

public class RealTimeAStarAgent extends SearchAgent{


    public RealTimeAStarAgent(SimulatorContext context, int maxExpand) {
        super(context, maxExpand);
    }
}
